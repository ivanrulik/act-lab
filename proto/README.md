# Recording schemas

Versioned Protobuf definitions for raw MCAP channels will be introduced in PR 6.
Schema changes must be backward compatible or include an explicit migration.
Generated source files should be produced during the build, not edited manually.

